---
import Layout from '../layouts/Layout.astro';
import Vectorify from '../components/Vectorify.astro';
import { config } from '../config';
---

<Layout title="Vectorize Your Photo - Magnetic Personalities">
  <main class="container mx-auto px-4 py-8">
    <div class="text-center mb-8">
      <h1 class="text-3xl font-bold mb-2">Photo Vectorizer</h1>
      <p class="text-gray-600">Upload your photo and convert it to a clean, scalable SVG vector</p>
    </div>

    <div class="max-w-4xl mx-auto bg-white rounded-lg shadow-md p-6">
      <Vectorify />
    </div>

    <div class="mt-8 max-w-4xl mx-auto">
      <h2 class="text-2xl font-semibold mb-4">How It Works</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="bg-white p-5 rounded-lg shadow-sm">
          <div class="text-blue-600 font-bold text-xl mb-2">1. Upload</div>
          <p>Upload any photo of yourself or someone else. The system works best with clear, front-facing portraits.</p>
        </div>
        <div class="bg-white p-5 rounded-lg shadow-sm">
          <div class="text-blue-600 font-bold text-xl mb-2">2. Vectorize</div>
          <p>Our system automatically converts your photo into a clean SVG vector with adjustable detail levels.</p>
        </div>
        <div class="bg-white p-5 rounded-lg shadow-sm">
          <div class="text-blue-600 font-bold text-xl mb-2">3. Download</div>
          <p>Download your vectorized image as an SVG file that you can use for your magnetic personality marker.</p>
        </div>
      </div>
    </div>

    <div class="mt-8 max-w-4xl mx-auto text-center">
      <a href="/" class="inline-block px-6 py-3 bg-blue-600 text-white rounded-md hover:bg-blue-700">
        Back to Home
      </a>
    </div>
  </main>
</Layout>

<script define:vars={{ cloudinaryConfig: config.cloudinary }}>
  // Make Cloudinary config available to client-side JavaScript
  window.CLOUDINARY_CONFIG = cloudinaryConfig;
</script>